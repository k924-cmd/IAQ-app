# 本地 V1 联调验收记录（2026-08-03）

## 1. 记录信息

| 项目 | 结果 |
|---|---|
| 候选基线 | `main@859a6ee` |
| 前端提交/构建 | `main@859a6ee`，本地联调候选，未单独产出构建编号 |
| 后端提交/构建 | `main@859a6ee`，本地联调候选，未单独产出构建编号 |
| 共享契约版本 | `1.0.0`，包含事件契约收口提交 `9419a34` |
| 数据与执行来源 | 仅 Mock/Replay，无外部模型、真实设备或生产服务调用 |
| 验收时间 | 2026-08-03（Asia/Shanghai） |
| 自动测试 | 后端 89/89；前端 10/10 |
| 真实浏览器联调 | `http://127.0.0.1:4173`，连接本地后端；仍仅使用 Mock 数据 |
| 产品结论 | 最终只读复核通过，无 P0/P1 |
| 总结论 | **本地 V1 验收通过**；不代表真实模型、真实设备、生产上线或真实收益验证 |

### 证据说明

- 本轮在同步 `main@859a6ee` 后重新执行 `backend` 的 `npm.cmd test` 和 `frontend` 的 `npm.cmd test`，终端结果分别为 89/89 和 10/10；未另存测试输出文件。
- 测试对应代码与覆盖说明位于 `backend/tests/*.test.js`、`backend/tests/ac-coverage.md`、`frontend/tests/*.test.js` 和 `frontend/tests/README.md`。
- 协调窗口确认的真实浏览器结果：2026-08-03 访问 `127.0.0.1:4173` 已连接本地后端；bootstrap 空气评分为 88，来源为 Mock；环境查询返回 `environment_status` 且来源为 Mock；控制台无错误。未提供截图文件，本记录不伪造截图路径。
- 产品最终只读复核结论由协调窗口确认；未提供独立的仓库内产品报告文件。
- `[x]` 只表示有当次自动测试、真实浏览器联调或产品最终复核的直接证据；`[ ]` 表示本轮未完成对应的单独演练，不得对外声称已测。

## 2. 前置条件

- [x] 前端、后端和共享契约锁定为 `main@859a6ee` / v1.0.0。
- [x] 后端测试仅使用确定性 Fake/Mock/Replay，实浏览器仅连接本地 Mock 后端，无外部调用。
- [x] 后端自动测试确认监听地址为 `127.0.0.1` 且 CORS 只接受契约允许来源；真实浏览器从 `127.0.0.1:4173` 连接成功。
- [x] 自动测试使用合成标识和内容，安全用例验证合成敏感值不进入公开结果或事件；本轮未读取真实凭据。

## 3. HTTP 与可信状态

| 用例 | 结果 | 直接证据与边界 |
|---|---|---|
| Health | 通过 | 后端 89/89 中 `GET health` 验证 200、v1.0.0、`local_mock` 和无内部信息；前端 10/10 验证 health 先于 bootstrap |
| Bootstrap | 通过 | 后端与前端自动测试覆盖；真实浏览器直接观察到评分 88 且来源为 Mock |
| 环境查询 | 通过 | 真实浏览器直接获得 `responseType=environment_status` 和 Mock 来源；后端 AC-003/020/022 自动测试覆盖指标、时间和来源 |
| AC-094 刷新/可信快照 | 通过（自动测试/产品复核） | 前端 10/10 覆盖 health/bootstrap 顺序、旧 Service Worker/缓存恢复与重载；产品最终复核通过。真实浏览器本轮未单独演练“改变后端快照后刷新对照” |
| 后端未连接 | 通过（自动测试） | 前端测试“API 不可用时明确降级为本地 UI Mock”与四页面边界测试覆盖；本轮真实浏览器未单独停止后端演练 |
| 结构化公开错误 | 通过（自动测试） | 后端验证结构化业务错误仍返回 HTTP 200；前端验证保留后端结构化响应 |
| 传输异常 | 通过（自动测试） | 后端覆盖 Content-Type、超限、无效 JSON、403、404、405、超时和未处理异常，并验证公开错误无堆栈 |
| 浏览器控制台 | 通过 | 2026-08-03 真实浏览器联调直接观察到控制台无错误；未提供截图文件 |

## 4. AC-090–094 前端交接

| AC | 结果 | 直接证据与边界 |
|---|---|---|
| AC-090 | 通过 | 前端自动测试确认快捷场景不映射后端任务；后端 AC-040–057 覆盖烹饪守护与三种模拟优化模式；产品最终复核通过。本轮未在浏览器逐一走完四种确认流程 |
| AC-091 | 通过 | 后端 AC-032–034、040–043、050–053、060–062、070–072 覆盖确认/取消语义；前端自动测试覆盖 `continuation`；产品最终复核通过。本轮未在浏览器分别操作所有确认按钮与纯文本路径 |
| AC-092 | 通过 | 前端自动测试直接验证 `scheduled/running/paused/stopped/failed` 的不同文字和图标；产品最终复核通过 |
| AC-093 | 通过 | 前端自动测试直接覆盖部分成功文案/图标；后端测试覆盖错误、拒绝、澄清、成功、部分成功与执行中语义；产品最终复核通过。本轮未在浏览器逐一触发六类结果 |
| AC-094 | 通过 | 前端自动测试覆盖可信 bootstrap 顺序、旧 Service Worker/缓存恢复和未连接降级；真实浏览器确认当次 bootstrap 已来自本地后端；产品最终复核通过。本轮未单独演练旧 Mock 状态对照 |

## 5. Mock/Replay 与声明边界

- [x] 真实浏览器直接显示 bootstrap 评分 88 / Mock；后端 AC-003/020/022 自动测试覆盖 Mock 环境指标、观测时间与来源。
- [ ] Replay 浏览器展示本轮未单独演练；后端 AC-053 只覆盖 Mock/Replay 任务来源语义，不将其写成已完成的前端展示验收。
- [x] 后端 AC-050–057 自动测试覆盖模拟优化摘要、创建、状态、来源和本轮无执行语义；产品最终复核通过。
- [x] 后端 AC-055 自动测试直接验证无合法候选动作时明确“本轮无执行”。
- [x] 后端 AC-056 自动测试拒绝真实 DQN/MQTT 和真实收益声明；产品最终复核通过；本轮仅 Mock/Replay 且无外部调用。

## 6. 执行回执与降级

| 用例 | 结果 | 直接证据与边界 |
|---|---|---|
| `succeeded` | 通过（自动测试） | 后端 AC-030/031 覆盖成功回执后的成功陈述；本轮浏览器未执行真实设备动作 |
| `noop` | 通过（自动测试） | 后端 AC-035 直接验证已是目标状态时不重复调用设备适配器 |
| `partial_success` | 通过（自动测试） | 后端 AC-047 覆盖逐项结果；前端 10/10 直接验证独立“部分成功”文案和图标 |
| `failed` | 通过（自动测试） | 后端 AC-002/038 验证失败回执不产生成功陈述 |
| `timed_out` | 通过（自动测试） | 后端 AC-002/038 验证超时回复明确表达超时或未知 |
| `unknown` | 本轮未单独演练 | 已有契约、运营文案和产品复核覆盖语义，但未找到当次前后端自动测试中对 `unknown` 执行回执的独立用例 |
| 模型不可用 | 通过（自动测试） | 后端 AC-080–082 直接覆盖独立降级与固定回复 |
| 设备服务不可用 | 通过（自动测试） | 后端 AC-083 直接覆盖禁止动作且聊天独立工作 |
| Mock/Replay 不可用 | 通过（自动测试） | 后端 AC-084 直接覆盖不创建新任务且不继续候选动作 |
| 任务替换中旧任务停止失败 | 通过（自动测试） | 后端 AC-062 直接验证停止失败时不创建新任务 |

## 7. 事件失败不影响主链路

- [x] 后端 AC-005 自动测试将事件发送器设为抛错，环境查询仍返回 `environment_status`，证明该查询链路不被事件异常阻断。
- [ ] 状态查询、单设备幂等控制、确认计划、任务状态迁移和多动作回执的完整对照矩阵本轮未单独演练。
- [ ] 事件发送“返回失败/抛异常/超时”三种故障注入本轮未全部演练；直接证据只覆盖抛异常。
- [ ] 事件失败是否进入独立、脱敏的可观测故障信号，本轮未单独验证。
- [ ] 本地 V1 未接入生产事件队列、重试或死信介质；无界重试检查本轮不适用，不记为已通过。

## 8. 敏感数据不落日志/事件

- [x] 后端 AC-005 使用合成敏感标记、电话和地址，验证它们不进入公开结果或事件。
- [x] 后端 AC-005 直接验证事件 `properties` 不含 `message`，且完整敏感对话不进入事件。
- [x] 后端 AC-005 直接验证合成电话、地址和敏感值不进入事件或公开错误。
- [x] 后端 HTTP 自动测试直接验证传输错误、超时和未处理异常不返回堆栈或内部信息。
- [ ] 应用日志、前端控制台、事件输出、重试/死信介质的跨介质唯一标记全量搜索本轮未单独执行；浏览器“控制台无错误”不等于该敏感标记搜索。
- [ ] 全部运行时事件通过 v1.0.0 `AnalyticsEvent` 完整 JSON Schema 验证本轮未单独执行；不以契约文档已收口代替运行时验证证据。
- [x] 本轮未读取、打印或验证任何真实环境变量凭据值。

## 9. 事件契约版本

- [x] `main@859a6ee` 包含事件契约收口提交 `9419a34`。
- [x] 共享 Schema 定义 `properties` 必需，并按事件定义必需字段、类型和枚举白名单。
- [x] `device_action_completed.executionSource` 只表示 `mock/device`；优化任务的 `mock/replay` 来源保存在 `AutomationTask.executionSource`。

## 10. 最终结论与未单独演练项

- [x] 后端自动测试 89/89 通过。
- [x] 前端自动测试 10/10 通过。
- [x] 2026-08-03 真实浏览器验证确认本地前后端已连接、bootstrap 评分 88/Mock、环境查询为 `environment_status`/Mock，且控制台无错误。
- [x] 产品最终只读复核通过，无 P0/P1。
- [x] 所有结果仅限本地 Mock/Replay，本轮无外部调用。
- [x] 本记录没有把未提供的截图、报告或文件路径写成已存在证据。

本轮未单独演练的项目为：Replay 浏览器展示、`unknown` 执行回执、事件失败完整矩阵、事件故障的独立可观测信号、跨介质敏感标记搜索以及运行时事件的完整 JSON Schema 验证。这些项目不改变本轮“产品最终只读复核通过、无 P0/P1”的已确认结论，但不得对外描述为已完成端到端演练。
