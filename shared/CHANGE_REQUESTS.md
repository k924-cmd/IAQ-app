# 跨线变更记录

## 状态

`proposed → reviewed → accepted → scheduled → implemented → verified → closed`

## 记录模板

| 字段 | 内容 |
|---|---|
| ID | `DEP-YYYY-MM-DD-NNN` |
| 创建日期 |  |
| 提出线 | Frontend / Backend / Product / Operations |
| 受影响线 |  |
| 变更类型 | API / Schema / Event / Product / Security / Release |
| 用户场景 |  |
| 当前契约 |  |
| 建议变更 |  |
| 兼容性影响 |  |
| 安全与隐私影响 |  |
| 优先级 | P0 / P1 / P2 / P3 |
| 负责人 |  |
| 验收条件 |  |
| 状态 | proposed |
| 目标版本 |  |
| 最后更新 |  |

---

## DEP-2026-08-03-001 · AI 小助手 V1 共享契约

| 字段 | 内容 |
|---|---|
| ID | `DEP-2026-08-03-001` |
| 创建日期 | 2026-08-03 |
| 提出线 | Product |
| 受影响线 | Backend / Frontend / Operations |
| 变更类型 | API / Schema / Event / Product / Security |
| 用户场景 | 聊天、可信状态查询、单设备控制、烹饪守护、模拟优化和唯一任务管理 |
| 当前契约 | v0.1 只覆盖最小消息、展示设备、环境快照和基础错误 |
| 建议变更 | 升级至 AI 小助手契约 `1.0.0`，新增计划、策略、确认、任务、回执、来源和脱敏事件语义 |
| 兼容性影响 | major 破坏性升级；旧 v0.1 客户端不得调用 V1 后端 |
| 安全与隐私影响 | 禁止模型直连执行器；确认绑定版本与有效期；事件不记录完整对话和敏感信息 |
| 优先级 | P0 |
| 负责人 | Coordination / Backend |
| 验收条件 | 产品 AC-000 至 AC-094；JSON Schema 可解析；后端与前端测试通过；本地 HTTP 和浏览器联调通过；产品最终复核无 P0/P1 |
| 状态 | closed |
| 目标版本 | AI Assistant V1 |
| 最后更新 | 2026-08-03 |

### 协调窗口评审结论

- 产品方案通过，作为 V1 目标规格，不代表当前能力已经实现。
- 输入上限为 4000 字符，环境新鲜度为 300 秒，确认有效期为 120 秒，幂等记录保留 24 小时。
- 首轮后端实现仅使用本地 Mock/Replay、固定规则和可替换模型适配器；禁止真实模型、真实 MQTT 和真实设备调用。
- 后端不得自行缩减产品验收条件；发现契约缺口时新增变更记录，由协调窗口修订。
- 传输层采用 `GET /v1/health`、`GET /v1/bootstrap` 和 `POST /v1/conversations/messages`；V1 为非流式 JSON，本地身份由服务器配置注入。

### 关闭记录

- 后端本地可信链路与 HTTP 适配已实现，自动测试 89/89 通过。
- 前端已接入 health、bootstrap 和结构化对话接口，自动测试 10/10 通过；断线时明确降级为 UI Mock。
- 真实浏览器联调确认本地后端连接、Mock 环境快照、`environment_status` 回复与零控制台错误。
- 事件契约已收口逐事件字段、类型和枚举，运营字典与发布检查已同步。
- 产品最终只读复核通过，无 P0/P1；正式证据及未单独演练项见 `operations/release/INTEGRATION_ACCEPTANCE_V1.md`。

---

## DEP-2026-08-03-002 · V1 接入真实模型 API：聊天与知识问答

| 字段 | 内容 |
|---|---|
| ID | `DEP-2026-08-03-002` |
| 创建日期 | 2026-08-03 |
| 提出线 | Product |
| 受影响线 | Backend / Frontend / Operations / Product |
| 变更类型 | API / Product / Security / Release |
| 用户场景 | 普通聊天；空气健康知识问答；本地规则无法可靠理解的自然语言；将可信状态与回执组织为自然回复 |
| 当前契约 | v1.0.0：`#chat` 丢弃模型输出、`#knowledge` 仅走固定模板；唯一模型实现为 `FakeModelAdapter`（`backend/src/adapters/fakes.js`），无真实模型适配器 |
| 建议变更 | 新增真实模型适配器（HTTP/SDK，可配置开关），接入 `#chat` 与 `#knowledge` 的回复生成；保留 Fake 与降级路径；模型不可用时沿用现有固定模板兜底；补充契约测试与降级测试 |
| 兼容性影响 | 目标保持 v1.0.0 契约不变；如 `SendMessageResponse.message.content` 与 `sources` 语义需升级，另行评估版本化；模型输出不改变设备、环境、策略、回执语义 |
| 安全与隐私影响 | 模型输出不得直接进入 Executor；设备、环境、策略、回执仍由固定代码决定；密钥不入库、不入日志；事件继续脱敏；真实模型调用必须获得用户明确授权 |
| 优先级 | P1 |
| 负责人 | Coordination / Backend |
| 验收条件 | 待产品线更新 AC 后补充；须包含自然语言回复、知识边界、医疗边界、降级口径 |
| 状态 | closed |
| 目标版本 | AI Assistant V1 |
| 最后更新 | 2026-08-03 |

### 协调窗口评审结论

- 依据独立详细 Roadmap 4.5 节与 9.1 节：V1 即应调用模型 API 实现普通聊天与空气健康知识问答；模型输出不得直接进入 Executor。
- 前置授权（开发前必须完成，未获授权不得接入真实模型调用）：
  1. 真实模型供应商与 API 端点；
  2. 密钥管理方式（密钥不得入库、入日志）；
  3. 调用成本上限；
  4. 网络策略（是否允许后端联网调用外部 API）。
- 状态流转待前置授权确认后推进 `reviewed → accepted`。

### 前置授权确认（2026-08-03）

- 供应商：DeepSeek；端点按供应商默认（`https://api.deepseek.com`），模型建议 `deepseek-chat`，最终模型名由后端实现时配置化确认。
- 密钥管理：不写入仓库、不入日志；由本地环境变量或独立本地配置文件提供，配置文件加入 `.gitignore`。
- 成本上限（默认，可调）：单次请求 `max_tokens` 上限 512；请求超时 15 秒；不启用自动重试；验收阶段调用次数受人工控制。
- 网络策略：允许本地开发时后端联网调用 DeepSeek API；默认直连，不强制代理（如有需要可通过环境变量配置代理）。
- 安全边界不变：模型输出不得直接进入 Executor；设备、环境、策略、回执仍由固定代码决定。
- 结论：前置授权满足，`DEP-2026-08-03-002` 推进为 `accepted`，进入派发执行。

### 共享契约评估（2026-08-03）

- `shared/contracts/ai-assistant-v1.schema.json` 的 `Source` 枚举已包含 `model`，`SourceRef.type` 可直接用于标记模型来源，无需新增枚举。
- `SendMessageResponse.message.content` 语义不变：始终是面向用户的展示文本；真实模型仅参与生成 `content`，不改写 `responseType`、`sources` 结构与业务字段。
- `sources` 保留原有含义：本次新增来源 `model` 与 `observedAt`（调用时间），`referenceId` 可省略或放稳定模型标识。
- 结论：共享契约 v1.0.0 **无需版本化升级**；后端实现按现有 Schema 输出即可。产品线仍需补充 AC（自然语言回复、知识边界、医疗边界、降级口径）。

### 关闭记录

- 产品线新增 AC-014～AC-019 并修订 AC-011～AC-013（见 `product/ACCEPTANCE_CRITERIA.md`、`product/decisions/D-2026-08-03-REAL-MODEL-CHAT-KNOWLEDGE-ACCEPTANCE.md`）。
- 后端线实现 `DeepSeekModelAdapter`（默认关闭、密钥本地注入、max_tokens≤512、超时 15s、不重试），接入 `#chat`/`#knowledge` 回复生成；模型输出经 `reply-safety.js` 文本边界守卫，不可用时回退固定模板；全量测试 103/103 通过。
- 路由修复：问候开头优先于通用知识兜底，`你好，简单介绍一下自己` 归为 `chat`；主题知识、急症、设备控制仍优先；新增路由测试。
- 前端线确认 v1.0.0 展示路径兼容 `model` 来源，补充断线/降级验证；测试 14/14 通过。
- 运营线更新事件口径与发布检查第 7 节（真实模型调用授权、密钥、成本上限、事件脱敏）。
- 真实模型调用已按授权完成一次本地验证：`demo:model` 返回 `sources[0].type === "model"`、`referenceId: "deepseek-chat"`，医疗边界声明保留；验证不构成设备/环境事实。
- 遗留：真实模型开放域回复的文本边界由固定正则守卫，后续按 AC-014～019 持续验收；发布前需在目标环境核验成本上限与密钥注入。

---

## DEP-2026-08-03-003 · 免责声明统一口径与展示边界（含 E3/E4）

| 字段 | 内容 |
|---|---|
| ID | `DEP-2026-08-03-003` |
| 创建日期 | 2026-08-04（登记） |
| 提出线 | Product |
| 受影响线 | Backend / Frontend / Operations / Product |
| 变更类型 | Product / Security / API（content 文本） |
| 用户场景 | 模型生成的聊天与知识回复统一附免责文案；健康/症状/医疗话题追加医疗强化句；实时天气或室外数值无可信来源时不编造；超长输入返回稳定错误 |
| 当前契约 | v1.0.0：无统一免责口径；无 E3/E4 边界 |
| 建议变更 | 通用免责 `Luna 是 AI 工具噢，我的回答仅供参考。`；健康话题追加 `以上仅为一般性信息，不构成医疗诊断，也不能替代专业医疗建议。`（追加关系，不替代通用免责）；E3 明确不可得或拒绝；E4 返回 `INPUT_TOO_LONG` 且先于语义路由、历史方案检查与模型调用 |
| 兼容性影响 | 仅 `content` 文本与行为调整；共享契约 v1.0.0 不升版本；E4 复用 `INPUT_TOO_LONG`，E3 复用拒绝语义 |
| 安全与隐私影响 | 医疗强化句不得替代通用免责；急性不适/危险暴露时安全引导优先；不编造实时外部数值；模板兜底同样遵守免责口径 |
| 优先级 | P1 |
| 负责人 | Coordination / Backend / Frontend |
| 验收条件 | 产品定稿 `2f36a4e`：新增 AC-095/096，修订 AC-010/011/012/019 |
| 状态 | accepted |
| 目标版本 | AI Assistant V1 |
| 最后更新 | 2026-08-04 |

### 协调窗口评审结论

- 产品定稿完整（`D-2026-08-04-DISCLAIMER-AND-BOUNDARY-UNIFICATION.md`），无需等待。
- 共享契约无需新增字段：免责为 `content` 文本；E4 复用 `INPUT_TOO_LONG`；E3 复用拒绝语义。
- 派发后端线（文案模板、E3/E4 边界）与前端线（展示样式）并行实现。

---

## DEP-2026-08-03-004 · 智能窗户免确认直接执行

| 字段 | 内容 |
|---|---|
| ID | `DEP-2026-08-03-004` |
| 创建日期 | 2026-08-04（登记） |
| 提出线 | Product |
| 受影响线 | Backend / Frontend / Product / Operations |
| 变更类型 | Product / Security / API（行为调整） |
| 用户场景 | 用户明确请求开关智能窗户时直接执行，减少确认步骤 |
| 当前契约 | v1.0.0：智能窗户状态改变必须确认（AC-032/033/034） |
| 建议变更 | 免确认直接执行；保留设备能力校验、状态版本校验、策略裁决、可信执行回执与来源标识 |
| 兼容性影响 | 破坏性规则变更（移除确认步骤）；`SendMessageResponse` 结构不变，共享契约不升版本 |
| 安全与隐私影响 | 移除确认降低事前知情/否决机会；保留执行校验与可信回执；残余风险缓解：状态可查询、来源随回执展示、真实设备接入时重新评估确认策略 |
| 优先级 | P1 |
| 负责人 | Coordination / Backend |
| 验收条件 | 产品定稿 `2f36a4e`：修订 AC-032/033/034/085 |
| 状态 | accepted |
| 目标版本 | AI Assistant V1 |
| 最后更新 | 2026-08-04 |

### 协调窗口评审结论

- 产品定稿完整（`D-2026-08-04-SMART-WINDOW-NO-CONFIRMATION.md`），含安全边界降低评估与缓解措施。
- 共享契约无需新增字段：免确认不改响应结构，确认管理仍用于任务与替换场景。
- 派发后端线（策略/确认链路调整）与前端线（确认卡片不再拦截窗户控制）并行实现。

---

## DEP-2026-08-03-005 · V1 已定义行为缺陷修复（非新需求）

| 字段 | 内容 |
|---|---|
| ID | `DEP-2026-08-03-005` |
| 创建日期 | 2026-08-04（登记） |
| 提出线 | Product |
| 受影响线 | Backend / Frontend / Product |
| 变更类型 | Product / API（缺陷修复，非新需求） |
| 用户场景 | 修复 V1 已定义行为的实现缺陷：多设备识别、澄清选项链路、回执中文化、观测时间可读化、聊天滚动与增量渲染 |
| 当前契约 | v1.0.0 已定义上述行为，但实现存在缺陷 |
| 建议变更 | 多设备识别→AC-039；澄清选项链路→AC-075/091；呈现口径子项（回执中文化、观测时间可读化）待产品线补充一句呈现口径，不阻塞实现；聊天滚动/增量渲染为纯前端体验优化，按协调窗口描述验收 |
| 兼容性影响 | 无契约变更；澄清链路复用 `options` 字段 |
| 安全与隐私影响 | 多设备识别沿用拆分/澄清边界；澄清选项链路沿用既有安全校验，无新增暴露 |
| 优先级 | P2 |
| 负责人 | Coordination / Backend / Frontend |
| 验收条件 | 引用既有 AC-039、AC-075/091；呈现口径子项待产品线补充；滚动/增量按协调窗口描述 |
| 状态 | accepted |
| 目标版本 | AI Assistant V1 |
| 最后更新 | 2026-08-04 |

### 协调窗口评审结论

- 本变更登记为「V1 已定义行为缺陷修复」，不以新需求登记；状态流转 `proposed → reviewed → accepted`，实现与 AC 补充并行，不再等待完整产品定稿。
- 共享契约无需新增字段：澄清复用 `options`；其余为 `content` 文本与前端呈现。
- 呈现口径子项（回执中文化、观测时间可读化）标注“待产品线补充一句呈现口径，不阻塞实现”。
