# AGENTS.md — 呼吸森林四线协作规则

本文件适用于整个仓库。各工作线开始任务前必须阅读本文件，以及本线目录中的说明文档。

## 1. 当前项目基线

- 当前主线只保留现有 UI 作为产品与视觉基线。
- AI 对话、设备接入、后端服务、数据链路和运营能力均按新需求重新设计与实现。
- 旧 AI V1、Phase A、DQN、历史验收测试、旧报告和旧构建产物已退出当前项目，不得恢复为开发依据或验收基线。
- Mock、演示数据和本地测试结果不得描述为真实设备、生产 AI 或正式上线结果。

## 2. 工作线与分支所有权

| 工作线 | 分支 | 默认可写范围 |
|---|---|---|
| 前端 | `codex/frontend-ui` | `frontend/**` |
| 后端 | `codex/backend-core` | `backend/**` |
| 产品 | `codex/product-spec` | `product/**` |
| 运营 | `codex/operations` | `operations/**` |
| 协调/集成 | `main` | `shared/**`、根目录治理文件、跨线集成 |

- 各工作线不得修改其他工作线目录。
- `shared/contracts/**`、`shared/CHANGE_REQUESTS.md` 和根目录治理文件由协调窗口最终维护。
- 工作线如需修改共享内容，只能提交变更建议，不得在本线分支单方面落地。
- 用户明确授权的临时跨线任务，必须先说明影响范围，再由协调窗口安排。

## 3. 产品、契约与实现顺序

涉及用户行为或跨线能力时，默认顺序如下：

1. 产品线定义用户场景、行为边界、异常路径与验收条件。
2. 协调窗口评审跨线影响，并在 `shared/CHANGE_REQUESTS.md` 登记。
3. 受影响工作线共同确认 API、Schema、事件和错误语义。
4. 协调窗口更新并版本化 `shared/contracts/**`。
5. 前端与后端基于同一契约并行实现和测试。
6. 产品线按原始验收条件验收，运营线核对事件、文案和发布要求。

技术可行性不明确时，允许在本线做限时探索；探索结果不能直接替代产品定义、共享契约或正式实现。

## 4. 跨线变更机制

- 跨线依赖统一登记在 `shared/CHANGE_REQUESTS.md`。
- 状态流转为：`proposed → reviewed → accepted → scheduled → implemented → verified → closed`。
- 未达到 `accepted` 的变更不得作为其他工作线的稳定依赖。
- 破坏性变更必须说明兼容策略、迁移方案、回滚方式和受影响版本。
- 协调窗口负责同步结论、解决冲突并决定合并顺序。

## 5. 实现与验证纪律

- 前端通过服务接口访问数据，不依赖后端内部模块。
- 后端不得把模型输出直接当作可信指令或可执行结果。
- 产品需求必须包含可验证的验收条件，不以后端当前实现反向改写需求。
- 运营事件不得包含密钥、完整敏感对话或未脱敏个人信息。
- 每条线只运行本线必要的测试；涉及共享契约时增加契约测试或兼容性验证。
- 不通过删除失败用例、降低断言或修改验收目标来制造“测试通过”。
- 不提交临时日志、缓存、构建产物、真实凭据或本地环境文件。

## 6. 安全与数据规则

- 永不输出、记录或提交 API Key、Secret、Token 或其他凭据内容。
- 不查看或请求任何包含 `KEY`、`SECRET`、`TOKEN` 的环境变量值。
- AI 输入输出、设备数据和用户信息遵循最小收集原则。
- 中文文本、JSON 和 JSONL 必须显式使用 UTF-8。
- 对外部模型、真实设备、生产服务或联网资源的调用，必须获得用户明确授权并说明影响。

## 7. 开工与交付要求

- 开工前确认当前分支、任务范围、依赖契约和验收条件。
- 发现规则冲突、跨线写入或契约缺失时立即停手，并报告协调窗口。
- 完成后报告修改范围、测试结果、遗留风险和需要其他工作线处理的事项。
- 未经用户明确要求，不自行合并到 `main`、强制推送或删除分支。
